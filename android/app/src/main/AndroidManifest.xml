<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Android 13+ требует это разрешение для показа уведомлений
         (в т.ч. push'ей FCM). Без него FirebaseMessaging.getToken()
         работает, но системно уведомления не показываются. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="SimbA"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:screenOrientation="portrait"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Канал уведомлений по умолчанию для FCM. Когда приложение в
             фоне/убито, баннер рисует система Android по данным от FCM, и
             берёт этот канал. Должен совпадать с каналом, который
             создаётся в коде (push_handler: 'simba_default'), иначе на
             Android 8+ часть фоновых пушей уходит в дефолтный системный
             канал без наших настроек важности/звука. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="simba_default" />
        <!-- Иконка уведомления. Android для статус-бара использует
             монохромный силуэт; пока берём launcher-иконку (на старых
             версиях может рисоваться как белый квадрат — для отдельной
             монохромной иконки нужен drawable ic_stat_notify). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: проверка доступности гео- и web-схем. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <!-- url_launcher по умолчанию шлёт ACTION_VIEW для tel:.
             Только DIAL без VIEW на части Android 11+ устройств приводит
             к canLaunchUrl == false → кнопка «Позвонить» не работает. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <!-- Deeplinks мессенджеров: нужны для canLaunchUrl/launchUrl
             через url_launcher. Без них на Android 11+ canLaunchUrl
             вернёт false, и наш fallback на https-ссылку не сработает. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="whatsapp"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tg"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="max"/>
        </intent>
        <!-- Deeplinks карт: чтобы кнопка «Построить маршрут» открывала
             выбранное приложение (Яндекс/2ГИС/Google), а не дефолтное.
             Без queries Android 11+ блокирует видимость других приложений,
             и launchUrl бросает PlatformException вместо false — наш
             try/catch ловит, но затем web-fallback тоже может уйти не туда. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="yandexmaps"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="dgis"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="comgooglemaps"/>
        </intent>
        <!-- Известные package-имена клиентов — для visibility в Android 11+. -->
        <package android:name="com.whatsapp"/>
        <package android:name="com.whatsapp.w4b"/>
        <package android:name="org.telegram.messenger"/>
        <package android:name="org.telegram.messenger.web"/>
        <package android:name="ru.oneme.app"/>
        <package android:name="ru.yandex.yandexmaps"/>
        <package android:name="ru.dublgis.dgismobile"/>
        <package android:name="com.google.android.apps.maps"/>
    </queries>
</manifest>
